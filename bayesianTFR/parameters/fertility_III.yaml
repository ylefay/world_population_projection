world:
  mu: 2.1 # long-term fertility rate
  rho: 0.89 # autocorrelation for the AR
  sigma_b: 0.10 # standard deviation of the AR
  #### Fitting an Exponential distribution on the initial fertility, TFR = 2 - Exp(Lambda) with
  scale: 0.04029496402877704 # scale parameter of the exponential (pdf. prop to exp(-x/scale))
  mu_bar_up: 2.1 # upper bound for the uniform prior on mu_bar
  sigma_mu_up: 0.318 # upper bound for the uniform prior on sigma_mu
  sigma_rho_up: 0.289 # upper bound for the uniform prior on sigma_rho
  # for technical reason, we initialize default values for the following conditional priors
  # mu_bar: 2.1 #1.05
  # sigma_mu: 0.318 #0.159
  # sigma_rho: 0.289 #0.1445
  # rho_bar: 1.0 #0.5"""